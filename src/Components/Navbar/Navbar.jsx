const Navbar = () => {
  return (
    <nav
      className="w-full h-[128px] bg-white px-[50px] flex items-center gap-[75px] font-nunito font-semibold text-[20px] text-[#00465F]"
      style={{ lineHeight: "100%", letterSpacing: "0%" }}
    >
      <span>Dashboard</span>
      <span>Dashboard</span>
      <span>Dashboard</span>
      <span>Dashboard</span>
    </nav>
  );
};

export default Navbar;
